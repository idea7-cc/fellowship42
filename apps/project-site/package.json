{
  "name": "@fellowship42/project-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public Fellowship42 project and community site",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "deploy:dry-run": "pnpm build && wrangler deploy --dry-run --config wrangler.jsonc",
    "deploy": "pnpm build && wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@fellowship42/brand": "workspace:*",
    "astro": "^7.3.3",
    "@astrojs/react": "^6.0.6",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "~6.0.3",
    "wrangler": "^4.133.0"
  }
}
